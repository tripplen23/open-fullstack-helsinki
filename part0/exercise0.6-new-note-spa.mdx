sequenceDiagram
    participant browser
    participant server
    participant database

    browser->>browser: The user types a new note into the input field and clicks the "Add" button

    Note right of browser: The JavaScript code sends a POST request to the server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa { "content": "new note" }
    activate server

    Note right of server: The server receives the new note from the client and inserts it into the database

    server->>database: INSERT INTO notes (content, date) VALUES ("new note", "2023-03-23")
    activate database
    database-->>server: success
    deactivate database

    server-->>browser: HTTP 201 created with the new note
    deactivate server

    Note right of browser: The JavaScript code receives the new note from the server and updates the app with it

    browser->>browser: The JavaScript code updates the app with the new note
